# Point the frontend at your backend API (no trailing slash).
# If unset, requests go to the same origin as the app (e.g. /api/v1).
VITE_API_BASE_URL=http://localhost:8001/api/v1

# Mapbox GL access token (required for the /map view).
VITE_MAPBOX_TOKEN=your_mapbox_token_here
